dist: focal
language: node_js
node_js: 
  - 16

before_install:

os:
  - linux
  - osx
  - windows

script:
  - npm t
  - npm run test-templates
  - npx scrypt-cli -v
  - npx scrypt-cli project my-proj
  - cd my-proj && npm i && npm t

